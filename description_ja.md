# コード説明文


---

## 1 概要

本モデルは、WCG (Woman-Child-Group) モデル（図1）と XGBoost を組み合わせた予測モデルである。WCG モデルは、家族単位で女性と子供（男の子）の生存率をグループ情報に基づいて予測するモデルであり、これを1つの特徴量として追加した。この特徴量に加え、7つの特徴量を組み合わせて XGBoost を使用して、生存（Survived）を予測した。

---

## 2 WCG モデルの概要と実装

WCG モデルの主な仮説は以下の通り：

- グループ内で女性と男の子が生存している場合、男の子も生存している可能性が高い。
- グループ内で女性と男の子が全員死亡した場合、女性も死亡している可能性が高い。

この仮説を実現するため、以下のステップで特徴量を作成した：

1. 「Name」列から苗字を抽出して「Surname」列を作成。
2. 「Surname」「Pclass」「Ticket番号（末尾を除く）」「Fare」「Embarked」を組み合わせて「GroupId」を作成。
3. グループ人数をカウントし、1人以下または男性（Title=man）の場合は「noGroup」として分類。
4. 「Pclass」「Ticket番号（末尾を除く）」「Fare」「Embarked」を組み合わせて「TicketId」を作成。Surnameは異なるが、グループに属していると考えられる親戚などもグループに含めるため。
5. グループごとの生存率を「GroupSurvived」として算出。
6. 訓練データにおいて、GroupId: GroupSurvived の辞書を作成。
7. 作成した辞書に基づいて、テストデータの GroupId に対応する GroupSurvived を代入する。
8. 生存率が不明なグループは、Pclass が 3 なら GroupSurvived に 0 を、それ以外なら 1 を代入する（3等客室は生存率が低いため）。
9. WCG 変数の作成手順：まず全行を 0 で初期化（全員生存しないと仮定）。次に女性（Title=woman）は全員 WCG=1（生存予測）で初期化。その後、女性（Title=woman）かつ GroupSurvived=0（グループが全滅）の場合は WCG=0（死亡予測）に変更、男の子（Title=boy）かつ GroupSurvived=1（グループ全員生存）の場合は WCG=1（生存予測）に変更した。なお、GroupSurvived 変数はモデルに加えても精度が向上しなかったため、最終モデルには含めなかった。

---

## 3 使用した説明変数

以下の特徴量を作成し、モデルに入力した：

| 特徴量 | 説明 |
|---|---|
| Fare | 運賃をそのまま使用 |
| TicketFreq | 同じチケット番号を持つ乗客数 |
| WCG | WCGモデルによる生存予測（1=生存予測、0=死亡予測） |
| FamilySize | SibSp + Parch + 1 |
| Sex_female | 性別が女性かどうかのダミー変数 |
| Pclass_1〜3 | Pclassをone-hot encodingで処理した変数 |

---

## 4 モデルとパラメータ設定

最終モデルには XGBoost を採用した。使用したパラメータは以下の通り：

- `max_depth=3`
- `learning_rate=0.1`
- `n_estimators=80`

他の手法（サポートベクターマシンや多層パーセプトロンなど）をアンサンブルしたが、正解率が向上しなかったため XGBoost のみを採用した。

---

## 5 工夫した点

「グループで乗船しており全員亡くなっていたら、テストデータでも亡くなっているだろう」という仮説を実装したかったが、実装方法がわからなかった。Kaggle のノートブックを調べていたところ、類似のアプローチを発見したのでそのコードを参考にした（[Chris Deotte 氏のノートブック](https://www.kaggle.com/code/cdeotte/titanic-wcg-xgboost-0-84688)）。

参考ノートブックでは男性のみを対象とした予測が行われていたが、データの不均衡の影響で精度が向上しなかった。そのため、本モデルでは WCG モデルに基づく予測を特徴量として追加した。単独乗船の女性を示す変数も試行したが、精度が向上しなかったため最終モデルから除外した。

---

## 図1: WCG モデルの判定フロー

```
乗客は男性（成人）か？
│
├─ はい（成人男性）
│   └─ WCG = 0（死亡と予測）
│
└─ いいえ（女性 または 男の子）
    │
    ├─ 女性の場合
    │   ├─ デフォルト: WCG = 1（生存と予測）
    │   └─ グループ内の全女性・男の子が死亡（GroupSurvived = 0）
    │       └─ WCG = 0（死亡と予測）
    │
    └─ 男の子の場合
        ├─ デフォルト: WCG = 0（死亡と予測）
        └─ グループ内の全女性・男の子が生存（GroupSurvived = 1）
            └─ WCG = 1（生存と予測）
```

> **参考ノートブック:** https://www.kaggle.com/code/cdeotte/titanic-wcg-xgboost-0-84688
